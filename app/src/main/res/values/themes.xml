<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LoginRegister" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:itemTextAppearance">@style/OptionsMenuTextAppearance</item>

    </style>
    <style name="OptionsMenuTextAppearance">
        <item name="android:textColor">@color/design_default_color_primary</item>
        <item name="android:textStyle">bold</item>
        <item name="backgroundColor">#FFC107</item>
    </style>
    <style name="Theme.LoginRegister" parent="Base.Theme.LoginRegister" />
</resources>